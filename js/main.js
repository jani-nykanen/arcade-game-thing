/*! Main file
 * 
 * Main file. Runs application and does
 * pretty much nothing else. The function
 * 'main' is used as onload function by index.html
 *
 * @author Jani Nykänen
 * @version 1.0
 */
 

/*! Start function
 *
 * The function called when the page is loaded
 */
function start()
{
	// Run application
	var app = new Application();
	app.Run();
}